# This file lives in /etc/hostname.igc0

# Tell this interface to use a static IP (because it's the internal interface)
# This needs to match the subnet that you will be using in /etc/dhcpd.conf and /etc/pf.conf

inet 192.168.90.1 255.255.255.0 NONE

# EOF
